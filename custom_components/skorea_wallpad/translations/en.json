{
  "title": "SKA Wallpad",
  "config": {
    "abort": {
      "single_instance_allowed": "Already configured. Only a single configuration possible.",
      "no_devices_found": "No devices found on the network"
    },
    "step": {
      "confirm": {
        "description": "Do you want to start set up?"
      },
      "user": {
        "description": "Write your device's host address.",
        "data": {
          "host": "Host : Socket IP(192.168.x.x) or Serial port(/dev/ttyXXX).",
          "port": "Port : When host is Socket.(default:8899)"
        }
      }
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "SKA Wallpad Options.",
        "data": {
          "host": "Host : Socket IP(192.168.x.x) or Serial port(/dev/ttyXXX).",
          "port": "Port : When host is Socket.(default:8899)",
          "scan_list": "Scan List : Scan selected device with scan interval.",
          "scan_interval": "Scan Interval : Interval between wallpad scan.(sec)",
          "send_retry": "Send Retry : If send fails x times, remove from queue.",
          "send_interval": "Send Interval : Interval between packet send.(millisecond)"
        }
      }
    }
  }
}
